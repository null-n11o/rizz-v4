## ER図

```mermaid
erDiagram
    profiles ||--o{ daily_records : records
    profiles ||--o{ daily_goals : sets
    profiles ||--o{ goals : sets

    profiles {
        uuid id PK "Supabase auth.users.id と同一"
        uuid user_id FK "auth.users.id"
        text name "ユーザーネーム"
        text email "メールアドレス"
        text x_url "X(旧Twitter) URL"
        text avatar_url "プロフィール画像URL (未実装)"
        text theme_preference "light | dark"
        int language "0: 日本語, 1: English"
        timestamp_with_time_zone created_at
        timestamp_with_time_zone updated_at
    }

    daily_records {
        uuid id PK
        uuid user_id FK
        int approached "声かけ数"
        int get_contact "連絡先ゲット数"
        int instant_date "即日デート数"
        int instant_cv "即数(sex)"
        text game_area "場所 (未実装入力UI)"
        date game_date "記録日 (YYYY-MM-DD)"
        timetz game_time "記録時間 (未実装入力UI)"
        timestamp_with_time_zone created_at
        timestamp_with_time_zone updated_at
    }

    daily_goals {
        uuid id PK
        uuid user_id FK
        int approached_target "声かけ目標数"
        int get_contacts_target "連絡先ゲット目標数"
        int instant_dates_target "即日デート目標数"
        int instant_cv_target "即目標数"
        date goal_date "目標日 (YYYY-MM-DD)"
        timestamp_with_time_zone created_at
        timestamp_with_time_zone updated_at
    }

    goals {
        uuid id PK
        uuid user_id FK
        text period_type "weekly | monthly | yearly"
        int approached_target
        int get_contacts_target
        int instant_dates_target
        int instant_cv_target
        timestamp_with_time_zone created_at
        timestamp_with_time_zone updated_at
    }
```

## テーブル設計補足

### profiles
- Supabase Auth のユーザー作成時にトリガーで自動生成される（または手動insert）
- `id` は `auth.users.id` と同一の UUID
- `theme_preference`: デフォルトは `dark`
- `language`: デフォルトは `0`（日本語）

### daily_records
- 1ユーザー・1日につき1レコード（`user_id` + `game_date` でユニーク）
- カウンターはインクリメント/デクリメントで更新
- `game_area`、`game_time` は詳細情報入力UI実装後に使用予定

### daily_goals
- 1ユーザー・1日につき1レコード（`user_id` + `goal_date` でユニーク）
- ホーム画面のプログレスバー表示に使用

### goals
- 週次/月次/年次の目標を管理
- `period_type` の値: `weekly` | `monthly` | `yearly`
- 注意: 日次目標は `daily_goals` テーブルで管理（`goals` テーブルは使用しない）

## RLS ポリシー

全テーブル共通: ユーザーは自分のデータ（`user_id = auth.uid()`）のみ参照・作成・更新可能。
